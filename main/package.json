{
  "name": "mathtree",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "publish": "npm run build-dev && npm run build && npm run build-type && npm run jest",
    "publish_no_jest": "npm run build-dev && npm run build && npm run build-type",
    "build": "webpack --config webpack.edge.js",
    "build-dev": "webpack --config webpack.dev.js",
    "build-stable": "webpack --config webpack.stable.js",
    "build-type": "npx tsc --project tsconfig.d.json",
    "jest": "cross-env NODE_OPTIONS=--experimental-vm-modules jest ./src/Math",
    "jest_soil": "jest ./src/Soil -debug",
    "typedoc": "typedoc --out ../typedoc ./src/Math ./src/Pen",
    "esbuild": "esbuild src/index.ts --bundle --outfile=out.js"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/katex": "^0.11.0",
    "cross-env": "^7.0.3",
    "esbuild": "^0.14.7",
    "jest": "^27.3.1",
    "jest-extended": "^1.1.0",
    "jest-matcher-deep-close-to": "^3.0.2",
    "ts-jest": "^27.0.7",
    "ts-loader": "^9.2.6",
    "typedoc": "^0.22.9",
    "typescript": "^4.4.4",
    "webpack": "^5.64.2",
    "webpack-cli": "^4.9.1"
  },
  "dependencies": {
    "contract": "file:../packages/contract",
    "gauss": "file:../packages/gauss",
    "katex": "^0.13.23",
    "paint": "file:../packages/paint",
    "sapphire-js": "^1.0.58"
  }
}
