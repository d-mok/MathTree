{
  "name": "mathtree-client",
  "version": "0.0.6",
  "description": "a private package",
  "main": "lib/index.js",
  "types": "lib",
  "files": [
    "/lib"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "publish_package": "npm version patch --no-git-tag-version && npm publish --access public"
  },
  "prettier": {
    "tabWidth": 4,
    "semi": false,
    "singleQuote": true,
    "arrowParens": "avoid",
    "singleAttributePerLine": true
  }
}
